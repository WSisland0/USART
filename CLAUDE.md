你是一个资深 Python + Qt 工程师，请帮我开发一个 Windows 下运行的串口写入工具。

# 技术要求

使用：

- Python 3.12
- PySide6
- pyserial

项目结构：

SerialWriter/
│── main.py
│── requirements.txt
│
├── ui/
│ ├── industrial_window.py
│ ├── modern_window.py
│
├── core/
│ ├── serial_manager.py
│ ├── protocol.py
│ ├── logger.py
│ └── config_manager.py
│
└── assets/

代码要求：

- 模块化
- 每个文件职责清晰
- 有中文注释
- 使用 signal/slot
- UI 和逻辑分离
- 易于二次开发
- 稳定优先，不追求炫技

------

# 软件功能

开发一个串口参数写入工具。

用于 USART 串口发送固定格式数据帧。

协议：

FF FF [DATA] 00 FF

其中：

DATA 为 1 字节。

用户输入范围：

0~255

十进制输入。

发送时转换为十六进制字节。

例如：

输入：

128

发送：

FF FF 80 00 FF

输入：

5

发送：

FF FF 05 00 FF

------

# UI 要求

生成两套 UI：

1）工业风

特点：

- 灰白配色
- 方角按钮
- 大字号
- 工厂风格
- 清晰稳定

2）现代风

特点：

- 圆角
- 蓝白配色
- 更漂亮
- 阴影卡片风格

------

# 主窗口布局

从上到下：

# ================================================== 区域1：串口设置

包含：

端口号下拉框：

自动枚举：

COM1
COM2
COM3 ...

按钮：

刷新

波特率下拉：

9600
19200
38400
57600
115200

数据位：

5
6
7
8

校验位：

None
Even
Odd

停止位：

1
1.5
2

按钮：

打开串口
关闭串口

显示当前状态：

● 已连接
● 未连接

颜色区分。

------

# ================================================== 区域2：数据设置（主体）

中央大输入框：

默认：

128

字体很大。

至少 42px。

只能输入数字。

范围：

0~255

自动限制。

下面是按钮：

[-10] [-5] [-1] [+1] [+5] [+10]

点击只修改数值。

不会自动发送。

下面：

大发送按钮：

发送

发送后数值保持。

------

# ================================================== 区域3：帧展示区（只读）

实时展示：

FF FF [80] 00 FF

其中 DATA 高亮。

颜色突出。

当数值变化时实时刷新。

------

# ================================================== 区域4：日志区

类似串口助手。

可滚动。

自动滚到底。

支持颜色区分：

发送 TX：

蓝色

接收 RX：

绿色

错误 ERROR：

红色

系统 INFO：

灰色

格式：

[12:03:22] TX -> FF FF 80 00 FF
[12:03:23] RX <- OK
[12:03:24] INFO 串口已打开

按钮：

清空日志

保存日志

------

# 串口功能

serial_manager.py 实现：

scan_ports()
open()
close()
send(bytes)
read_thread()

后台线程持续读取。

收到数据显示到日志区。

异常处理完善：

串口占用
打开失败
发送失败
设备拔出

都要提示。

------

# 配置保存

保存最后一次：

COM口
波特率
数据位
校验位
停止位
UI风格

下次自动恢复。

config.json 保存。

------

# protocol.py

实现：

build_frame(value:int)->bytes

输出：

b'\xFF\xFF\x80\x00\xFF'

以及：

to_hex_string()

输出：

FF FF 80 00 FF

协议以后可能改变。

所以封装独立。

------

# 额外要求

增加顶部菜单：

视图 → 工业风 / 现代风

可以切换。

增加图标。

增加窗口标题：

Serial Writer Tool

默认窗口宽：

900

高：

700

可 resize。

------

请输出完整项目代码。

按文件分块输出。

先输出 requirements.txt 和 main.py。